name: Lint

on: push

jobs:
  lint:
    name: Run linters
    runs-on: ubuntu-latest

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4

      - name: Setup
        uses: ./.github/templates/nodeSetup.yml

      - name: Run linters
        run: yarn run lint
